# Mirror these versions with the Ampere Client release you are building against.
[versions]
mod-version = "1.0.0"

minecraft = "26.1.2"
fabric-loader = "0.18.4"
fabric-api = "0.147.0+26.1.2"
Ampere = "2.3"

loom = "1.16-SNAPSHOT"

[libraries]
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
fabric-loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric-loader" }
fabric-api = { module = "net.fabricmc.fabric-api:fabric-api", version.ref = "fabric-api" }
Ampere = { module = "com.ampere:Ampere", version.ref = "Ampere" }

[plugins]
fabric-loom = { id = "net.fabricmc.fabric-loom", version.ref = "loom" }
